# nutria-labII

Monorepo con Turborepo, React + Vite en `apps/web` y NestJS + Prisma en `apps/api`.

## Desarrollo

```bash
corepack pnpm install
corepack pnpm dev
```

Copiá `apps/api/.env.example` a `apps/api/.env` y completá las credenciales antes de usar Prisma o Gemini.
