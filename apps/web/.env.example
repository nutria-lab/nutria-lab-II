# URL base del backend. Obligatoria: la app no arranca sin ella (ver services/apiClient.ts).
# - Desarrollo local: http://localhost:3000 (o el puerto donde corras `pnpm --filter api dev`)
# - Producción y Preview (Vercel): /api, para usar el rewrite same-site de vercel.json.
#   Configurarlo explícitamente en el dashboard del proyecto para ambos entornos.
VITE_API_URL="http://localhost:3000"
